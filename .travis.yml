language: python
python:
 - "3.6"

# command to run tests

install:
  - pip install codecov

script:
  - python -m unittest TestTriangle
  - coverage xml
  - coverage report -m


after_success:
  - codecov
  - bash <(curl -s https://codecov.io/bash)
