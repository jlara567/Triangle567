language: python
python:
 - "2.7"

# command to run tests

install:
  - pip install codecov

script:
  - python -m unittest TestTriangle
  - coverage xml -i
  - coverage report


after_success:
  - bash <(curl -s https://codecov.io/bash)
